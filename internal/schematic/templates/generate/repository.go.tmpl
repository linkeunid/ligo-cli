package repository

import "{{.ModulePath}}/internal/domain/entity"

// {{.Pascal}}Repository defines the interface for {{.Kebab}} data access.
type {{.Pascal}}Repository interface {
	FindByID(id int) (*entity.{{.Pascal}}, bool)
	FindAll() []*entity.{{.Pascal}}
	Create(name string) *entity.{{.Pascal}}
	Update(id int, name string) (*entity.{{.Pascal}}, bool)
	Delete(id int) bool
}
